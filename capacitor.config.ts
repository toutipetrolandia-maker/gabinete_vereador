import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gabinetedigital.app',
  appName: 'GabineteDigital',
  webDir: 'dist'
};

export default config;
